title: 実装演習レポート
description: 実装演習レポートを作成します。
template:
  repo: '-template'
  name: ''
before:
  - type: createIssue
    title: Welcome!
    body: new-issue.md
steps:
  - title: First step
    description: The first step in your course
    event: issues.assigned
    link: '{{ repoUrl }}'
    actions:
      - type: closeIssue
